Lo que ya hicieron
1. Infraestructura base
.env configurado
conexion a PostgreSQL funcionando
servidor Fastify levantando bien
GET /health
banner de arranque
manejo global de errores

2. Gestion basica de personas
listar personas
contar personas
validacion de datos

Endpoints:

GET /health
GET /api/personas
GET /api/personas/count

3. Registro local por identificador unico
inicio de registro con un solo identificador
el identificador puede ser correo o numero de telefono
envio de codigo de verificacion por el canal correspondiente
verificacion del codigo antes de crear la cuenta
creacion final de cuenta solo despues de verificar y definir clave
soporte de preregistro temporal

Endpoints:

POST /api/auth/register/iniciar
POST /api/auth/register/verificar
POST /api/auth/register/establecer-clave

4. Autenticacion local
login local con un solo campo de acceso
el backend detecta si es correo o celular
hash de contraseña
verificacion de contraseña
control de intentos fallidos
bloqueo temporal basico
emision de accessToken
emision de refreshToken

Endpoints:

POST /api/auth/login-local

5. Sesiones y tokens
refresh token hasheado en BD
refresh con rotacion
revocacion del refresh viejo
logout
logout global
cierre de ingreso
consulta del usuario autenticado
middleware JWT
el JWT ahora incluye identificador y tipoIdentificador

Endpoints:

POST /api/auth/refresh
POST /api/auth/logout
POST /api/auth/logout-global
GET /api/auth/me

6. Historial de sesiones
listar mis sesiones
ver detalle de una sesion

Endpoints:

GET /api/auth/sessions
GET /api/auth/sessions/:idIngreso

7. Verificacion y recuperacion
verificacion de correo para cuentas que usan correo
recuperacion de clave por correo
cambio de clave autenticado

Endpoints:

POST /api/auth/verificar-correo/enviar
POST /api/auth/verificar-correo/confirmar
POST /api/auth/recuperar-clave
POST /api/auth/restablecer-clave
POST /api/auth/cambiar-clave

8. Roles y permisos
crear rol
listar roles
crear permiso
listar permisos
asignar rol a persona
quitar rol a persona
consultar roles de una persona
asignar permiso a rol
quitar permiso a rol
consultar permisos de un rol
roles y permisos dentro del JWT
middleware por rol
middleware por permiso
rutas protegidas de administracion

Endpoints:

GET /api/roles
POST /api/roles
GET /api/permisos
POST /api/permisos
GET /api/personas/:idPersona/roles
POST /api/personas/:idPersona/roles
DELETE /api/personas/:idPersona/roles/:idRol
GET /api/roles/:idRol/permisos
POST /api/roles/:idRol/permisos
DELETE /api/roles/:idRol/permisos/:idPermiso

Que ya pueden demostrar

Pueden decir que el microservicio:

autentica usuarios
permite registro con correo o telefono, pero usando solo uno
verifica el identificador antes de crear la cuenta
emite tokens para otros microservicios
valida tokens
maneja sesiones
permite logout y refresh
protege rutas
soporta autorizacion por roles y permisos

Que falta
1. Vincular un segundo identificador

Si mas adelante lo necesitan:

agregar correo a cuentas creadas con telefono
agregar telefono a cuentas creadas con correo
verificar el nuevo canal ya autenticado

2. Recuperacion por telefono

Todavia falta una version equivalente por SMS:

solicitud de recuperacion por celular
codigo por SMS
restablecimiento de clave con telefono

3. 2FA
activar/desactivar segundo factor
validar segundo factor

4. OAuth

La base lo soporta, pero falta implementarlo:

Google
Microsoft
Apple

5. Mejoras opcionales
reenvio de codigo de registro
expiracion y limpieza automatica de preregistros
administracion mas fina de permisos
auditoria consultable por endpoint
